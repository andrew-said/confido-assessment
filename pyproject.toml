[project]
name = "confido-assessment"
version = "0.1.0"
description = "dbt take-home: standardized invoice line items model"
requires-python = ">=3.10,<3.14"
dependencies = [
    "dbt-snowflake>=1.9,<1.11",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
